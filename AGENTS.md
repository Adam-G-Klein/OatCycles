# Agent instructions

See [CLAUDE.md](CLAUDE.md) — same rules for every agent, kept in one file so
they cannot drift apart.

The short version: open the app with `?agent=1`, drive it with
`window.oat.silentPlay(code)`, and never write to `SavedSongs/` or
`AutoSaves/`.
